import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    paddingVertical: 12,
    borderRadius: 8,
    alignItems: "center",
  },
  active: {
    opacity: 0.7,
  },
});
