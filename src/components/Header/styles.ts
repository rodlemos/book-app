import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    paddingTop: 48,
    paddingBottom: 16,
  },
  search: {
    marginHorizontal: 16,
    height: 44,
    backgroundColor: "#e8d7f1",
    borderRadius: 80,
    paddingHorizontal: 8,
    flexDirection: "row",
    alignItems: "center",
  },
  input: {
    flex: 1,
  },
});
